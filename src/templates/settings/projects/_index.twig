{% set title = 'Projects'|t('osim-focus') %}

{% if craft.osimFocus.accountOptions is empty %}
    {{include('osim-focus/settings/projects/_index-message')}}
{% else %}
    {{include('osim-focus/settings/projects/_index-results')}}
{% endif %}
